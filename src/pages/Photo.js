import React from 'react';
import styled from 'styled-components';
import PhtoView from '../components/PhotoView';

const PhotoBlock = styled.div``;

const Photo = () => {
return (
  <PhotoBlock>
    <PhtoView />
  </PhotoBlock>
  );
};

export default Photo;